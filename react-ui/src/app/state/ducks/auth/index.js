import * as actions from './actions'
import * as types from './types'
import * as selectors from './reducer'
import { default as reducer } from './reducer';

export { actions, types, selectors, reducer }
export default reducer
